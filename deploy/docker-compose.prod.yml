services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: opendataworks-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-opendataworks}
      MYSQL_USER: ${MYSQL_USER:-opendataworks}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-opendataworks123}
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./database/mysql:/docker-entrypoint-initdb.d:ro
    networks:
      - opendataworks-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot123"]
      interval: 10s
      timeout: 5s
      retries: 5
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password

  # 后端服务
  backend:
    image: ${OPENDATAWORKS_BACKEND_IMAGE:-mikefan2019/opendataworks-backend:0.8.0}
    container_name: opendataworks-backend
    restart: always
    env_file:
      - ./.env
    # DolphinScheduler 不包含在本 Compose 中，配置请在系统管理界面进行
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - opendataworks-network
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    volumes:
      - backend-logs:/app/logs

  # 前端服务
  frontend:
    image: ${OPENDATAWORKS_FRONTEND_IMAGE:-mikefan2019/opendataworks-frontend:0.8.0}
    container_name: opendataworks-frontend
    restart: always
    env_file:
      - ./.env
    ports:
      - "8081:80"
    depends_on:
      backend:
        condition: service_started
      dataagent-backend:
        condition: service_healthy
    networks:
      - opendataworks-network
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # DataAgent 后端服务（NL2SQL）
  dataagent-backend:
    image: ${OPENDATAWORKS_DATAAGENT_BACKEND_IMAGE:-mikefan2019/opendataworks-dataagent-backend:0.8.0}
    container_name: opendataworks-dataagent-backend
    restart: always
    user: "${DATAAGENT_RUNTIME_UID:-1000}:${DATAAGENT_RUNTIME_GID:-1000}"
    env_file:
      - ./.env
    environment:
      PYTHONUNBUFFERED: "1"
      HOME: /tmp/dataagent-home
      HOST: 0.0.0.0
      PORT: 8900
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${DATAAGENT_MYSQL_USER:-dataagent}
      MYSQL_PASSWORD: ${DATAAGENT_MYSQL_PASSWORD:-dataagent123}
      MYSQL_DATABASE: ${DATAAGENT_MYSQL_DATABASE:-opendataworks}
      SESSION_MYSQL_DATABASE: ${DATAAGENT_SESSION_MYSQL_DATABASE:-dataagent}
    ports:
      - "8900:8900"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - opendataworks-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8900/api/v1/nl2sql/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    volumes:
      - ${DATAAGENT_SKILLS_DIR:-../dataagent/.claude/skills}:/app/.claude/skills

networks:
  opendataworks-network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${OPENDATAWORKS_PROD_SUBNET:-10.71.0.0/24}
          gateway: ${OPENDATAWORKS_PROD_GATEWAY:-10.71.0.1}

volumes:
  mysql-data:
    driver: local
  backend-logs:
    driver: local
